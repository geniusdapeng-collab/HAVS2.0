# 专家反馈实施方案 - v6.6 内存优化补丁

## 实施状态：第一批（止血）已完成

根据专家提供的反馈，已完成以下内存优化修改：

---

## 1. 新增文件

### `systems/runtime-memory-guard.js`
内存监控工具，提供以下能力：
- 读取 cgroup 内存限制信息（兼容 cgroup v1/v2）
- 内存快照：RSS / heapUsed / external / arrayBuffers
- `memoryReliefPoint(tag, extra)` - 在关键节点打内存点并自动触发 GC
- `snapshotMemory()` / `logMemory()` / `dumpMemoryToFile()` - 记录到 `./output/memory-snapshots.log`

### `systems/pipeline-checkpoint.js`
断点续作机制，提供以下能力：
- `saveCheckpoint()` / `loadCheckpoint()` - 保存/加载中间状态
- `updateShotPromptIncrementally()` - 增量更新单个 shot 的 prompt
- `appendJsonl()` - 追加日志到 JSONL 文件

---

## 2. 修改文件

### `systems/llm-reasoning-engine.js`
**修改内容**：去掉 `raw: result` 返回
- 专家指出的"坑 1"：不要保存完整 API 原始响应
- 原代码返回了完整的 API 响应对象（包含 choices/usage 等），占用大量内存
- 现在只返回必要的字段：content / reasoning_content / tokenCount

### `run-preproduction-v3.js`
**修改内容**：启动时内存诊断
- 引入 `runtime-memory-guard`
- 启动时打 `bootstrap` 内存点
- 注册进程事件处理器：SIGTERM / uncaughtException / unhandledRejection

### `zhuoyue-system/core/nirath-master-pipeline.js`
**Stage 5A 修改**：
- 函数开头：`memoryReliefPoint('stage5A:start')`
- 每批次前：`memoryReliefPoint('stage5A:before-batch')`
- 每批次后：
  - `saveCheckpoint()` 落盘到 `stage5A-checkpoint.json`
  - `appendJsonl()` 记录到 `stage5A-batches.jsonl`
  - `memoryReliefPoint('stage5A:after-batch')`
  - 及时释放 `result` 对象
- 函数结尾：`memoryReliefPoint('stage5A:end')`

**Stage 11 修改**：
- 函数开头引入 `runtime-memory-guard` 和 `pipeline-checkpoint`
- 循环前：`memoryReliefPoint('stage11:start')`
- 每个 shot 处理前：`memoryReliefPoint('stage11:before-shot')`
- 循环结束后：`memoryReliefPoint('stage11:end')`
- 每个 shot 完成后：`updateShotPromptIncrementally()` 落盘到 `stage11-prompts.json`

---

## 3. 启动命令建议

当前系统内存：7.5GB，可用 5.6GB，无 cgroup 限制

**推荐启动命令**：
```bash
NODE_OPTIONS="--max-old-space-size=3072 --expose-gc" node run-preproduction-v3.js
```

说明：
- `--expose-gc`：**必须**，代码中使用了 `global.gc()` 来手动触发垃圾回收
- `--max-old-space-size=3072`：将 V8 堆上限设为 3GB（系统有 5.6GB 可用，留 2GB+ 给 RSS/native/buffer）

如果仍然出现 OOM，可以调整为：
```bash
NODE_OPTIONS="--max-old-space-size=4096 --expose-gc" node run-preproduction-v3.js
```

---

## 4. 监控与排查

运行后，查看以下文件来监控内存使用情况：

```bash
# 实时查看内存快照
tail -f ./output/memory-snapshots.log

# 查看 Stage 5A checkpoint
cat ./output/stage5A-checkpoint.json

# 查看 Stage 11 checkpoint
cat ./output/stage11-prompts.json

# 查看 Stage 5A 批次日志
cat ./output/stage5A-batches.jsonl

# 查看 Stage 11 shot 日志
cat ./output/stage11-shots.jsonl
```

如果再次出现 SIGKILL，查看 `memory-snapshots.log` 中最后一个快照：
- 如果 `rssMB` 接近 `cgroupLimitMB` → 容器内存限制问题
- 如果 `rssMB` 远小于系统内存 → 可能是 OpenClaw 子代理超时或其他原因

---

## 5. 尚未实施的（第二批，视情况启用）

如果第一批修改后仍然出现 SIGKILL，考虑实施：

### 方案 3：Stage 11 子进程隔离
- 每个 shot 用独立子进程生成 prompt
- 子进程结束后 OS 自动回收全部内存
- 这是专家推荐的"最稳"方案

### 更激进的内存释放
- 在 Stage 11 循环内，每个 shot 处理后清空所有中间变量
- 限制 referenceImages 大小（只保留路径，不保留完整对象）
- 限制 prompt 最大长度（当前已有限制 1500 字符）

---

## 6. 专家反馈原文要点回顾

**最可能根因**：
1. 容器 / cgroup 内存限制触发 → 但当前系统无 cgroup 限制
2. Node 进程 RSS 暴涨，不只是 heap → 已添加 RSS 监控
3. LLM 返回的 reasoning_content + 累积 → 已去掉 `raw: result`

**已修复的坑**：
- 坑 1：不要保存完整 API 原始响应 ✅
- 坑 2：不要在 storyboard 上挂大字段 → 已添加 checkpoint 及时落盘

---

*实施时间：2026-06-16*
*版本：v6.6-memory-patch-001*
